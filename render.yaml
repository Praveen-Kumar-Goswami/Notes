services:
  - type: web
    name: make-a-note
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        sync: false
      - key: FIREBASE_CREDENTIALS
        sync: false

